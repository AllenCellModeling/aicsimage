<?xml version='1.0' encoding='UTF-8'?>

<project name="aicsimage" basedir="." default="clean">

    <property environment="env"/>
    <!--
        CREDENTIALS FOR PUBLISHING TO ARTIFACTORY
        Required only on Jenkins
    -->
    <property file="${env.HOME}/.ivy2/.ant-credentials"/>

    <target name="init">
        <property name="basedir" location="${basedir}/../../"/>
        <property name="pipelinedir" location="${basedir}/pipeline"/>
        <property name="module.work.dir" location="${basedir}"/>
        <property name="module.buildfolder.dir" location="${module.work.dir}/build"/>
        <property name="module.dist.dir" location="${module.work.dir}/dist"/>
        <property name="module.egg_info.dir" location="${module.work.dir}/aicsimage.egg-info"/>
        <echoproperties prefix="module."/>
    </target>

    <target name="testing">
        <exec dir="." executable="python">
            <arg line='setup.py test'/>
        </exec>
    </target>

    <!-- ================================== -->
    <!-- TOP LEVEL TARGETS -->
    <!-- ================================== -->

    <target name="clean" depends="init">
        <delete dir="${module.buildfolder.dir}"/>
        <delete dir="${module.config.dir}"/>
        <delete dir="${module.egg_info.dir}"/>
        <echo message="clean complete!"/>
    </target>

    <target name="publish-snapshot" depends="testing">
        <echo message="testing complete!"/>
    </target>

</project>